language: cpp

compiler: g++

notifications:
  email: false
  
addons:
    apt:
      sources:
      - ubuntu-toolchain-r-test
      packages:
      - gcc-4.8
      - g++-4.8
      
install:
    - if [ "$CXX" = "g++" ]; then export CXX="g++-4.8" CC="gcc-4.8"; fi
  
script: g++ -std=c++11 -o chess *.cpp figures/*.cpp
